Requirement | Test | Status
---|---|---
`support_enabled` + `desktop_share_enabled` config keys added, `camera_enabled`/`audio_enabled`/`desktop_enabled` removed | `src/fork_config.rs::tests::parses_all_role_and_mode_combinations`, `accepts_either_flag_alone` | Implemented (Connection Workflow)
Reject configuration where both `support_enabled` and `desktop_share_enabled` are false | `src/fork_config.rs::tests::rejects_both_support_and_desktop_share_disabled` | Implemented (Connection Workflow)
Versioned configuration loading | `src/fork_config.rs::tests::parses_all_role_and_mode_combinations`, `rejects_unsupported_version`, `rejects_missing_required_field`, `rejects_malformed_toml` | Implemented (Phase 3)
Configuration validation (role, authentication.mode, listen_address, listen_port, video_quality, audio_quality, log_level) | `src/fork_config.rs::tests::rejects_invalid_role`, `rejects_invalid_auth_mode`, `rejects_invalid_listen_address`, `rejects_zero_listen_port`, `rejects_invalid_quality_and_log_level` | Implemented (Phase 3)
`role = local` -> outbound-only (upstream `is_outgoing_only()`) | `src/fork_config.rs::tests::apply_sets_outgoing_only_for_local_role` | Implemented (Phase 3)
`role = remote` -> inbound-only (upstream `is_incoming_only()`) | `src/fork_config.rs::tests::apply_sets_incoming_only_for_remote_role` | Implemented (Phase 3)
`authentication.mode` (ask/password/ask_and_password) -> upstream `approve-mode` (click/password/default) | `src/fork_config.rs::tests::apply_maps_authentication_modes_to_approve_mode_option` | Implemented (Phase 3)
Local initiates only; remote accepts sessions only | N/A yet — UI/product-level restriction (`docs/architecture.md` "Connectivity"), depends on the minimal-UI phase | Planned (later phase)
No relay/rendezvous in the user experience | N/A yet — UI-level restriction, upstream transport code untouched by design | Planned (later phase)
~~Camera/audio always start, desktop only when `desktop_enabled = true`~~ | superseded — see rows below | Superseded 2026-08-28 (Support/Desktop redesign)
~~Support button launches `DEFAULT_CONN` + `VIEW_CAMERA` together, audio via `DEFAULT_CONN`~~ | superseded — see rows below | Superseded 2026-08-28 (Voice Call confirmed standalone on VIEW_CAMERA)
Desktop button launches standard upstream `DEFAULT_CONN` only (no camera, no voice call); hidden when `desktop_share_enabled = false` | Implemented in `connection_page.dart` (`if (_desktopShareEnabled)`); not yet manually verified in a running app (Flutter build blocked, see environment issue) | Implemented, unverified end-to-end
Support button always launches `VIEW_CAMERA` + Voice Call; additionally `DEFAULT_CONN` when `desktop_share_enabled = true`; hidden when `support_enabled = false` | Implemented in `connection_page.dart` (`onSupport()`) + `view_camera_page.dart` (`sessionRequestVoiceCall` on first image); not yet manually verified in a running app | Implemented, unverified end-to-end
`support_enabled`/`desktop_share_enabled` each independently gate their own button; must not render when false | `src/fork_config.rs::tests::apply_maps_support_enabled_to_enable_camera_permission`, `apply_maps_desktop_share_enabled_to_local_option` (Rust side); Dart side uses `if (...)` (absent, not disabled) — not yet verified in a running app | Implemented, unverified end-to-end (Dart)
Remote rejects `VIEW_CAMERA`/Voice Call when `support_enabled = false` | `src/fork_config.rs::tests::apply_maps_support_enabled_to_enable_camera_permission` | Implemented (Connection Workflow)
`desktop_share_enabled = false` remote-side enforcement | **Not implemented — documented gap.** No existing upstream permission rejects `DEFAULT_CONN` outright; adding one would be new authentication code. Enforced locally only. | Investigated, gap documented (`docs/FORK_PROFILE_SPEC.md`)
No audio-service/server-side media modifications required for Support | Verified by investigation, not by a fix — `docs/session-orchestration-analysis.md` §7 (audio-on-camera plumbing exists but unused) and §9-10 (Voice Call confirmed to work standalone on VIEW_CAMERA, no DEFAULT_CONN needed) | Investigated and documented, no code change needed
Clean build of the full `rustdesk` binary | Blocked — see `docs/UPSTREAM_UPGRADE_GUIDE.md` "Known build environment issue" (pre-existing vcpkg/aom/nasm incompatibility, unrelated to this phase's code) | Blocked (environment, not code)
